apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ecommerce-dev-tls
  namespace: default
spec:
  secretName: ecommerce-dev-tls
  commonName: ecommerce.dev.local
  dnsNames:
    - ecommerce.dev.local
  issuerRef:
    name: dev-cluster-issuer
    kind: ClusterIssuer
